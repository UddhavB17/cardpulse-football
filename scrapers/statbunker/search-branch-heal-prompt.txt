Fix /usual/search: read comps_id (not comp_id) from input URL query; allowed 745,596,776,791. Require exactly one player link whose visible text equals search param (case/diacritic insensitive). Extract numeric player_id; set resolved_player_name, resolved_player_id, resolved_player_url on every row; canonical source_url is /players/SeasonMatches?comps_id={comps_id}&comps_type=EPL&player_id={id}. Extract every table.table tbody match row (competition,home_team,score,away_team,started,substitute,minutes_played,goals,assists,yellow_cards,second_yellow_cards,red_cards,played_on). PlayerStandings unchanged: every tbody row, comp_id from input URL maps 745=2023,596=2024,776=2025,791=2026; td1..9 fields; player_url=null ok. Fail closed on ambiguous search or wrong season. No pagination.
