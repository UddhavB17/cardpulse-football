Bug: search URLs die with "Invalid comp_id: null" because comp_id is validated before branching. Branch FIRST on input.url query; never validate comp_id up front. SEARCH branch when search+comps_id present: comps_id must be 745/596/776/791 else bad_input. normalize=lowercase+strip diacritics. navigate(input.url); anchors having player_id= in href are plain links outside table.table; several share one id. Keep those whose normalized text == normalize(search); dedupe; EXACTLY ONE unique id else bad_input. seasonUrl="https://www.statbunker.com/players/SeasonMatches?comps_id="+comps_id+"&comps_type=EPL&player_id="+id; navigate it; extract EVERY table.table tbody row: competition,home_team,score,away_team,started,substitute,minutes_played,goals,assists,yellow_cards,second_yellow_cards,red_cards,played_on. EVERY row gets resolved_player_name, resolved_player_id, resolved_player_url=getPlayerStats absolute URL, source_url=seasonUrl. 0 rows=>bad_input. STANDINGS (comp_id) unchanged.